#include <iostream>
#include <iomanip>

using namespace std;

int main() {
        // 0
        int 5a = 20;
        
        // 1
        string name = "John";

        // 2 
        string _name = "John";
        
        // 3
        string name1 = "John";

        // 4
        string name! = "John";

        // 5
        string !name = "John";

        // 6
        int number = 20;

        // 7
        int _number = 20;

        // 8
        int @number = 20;

        // 9
        int number@ = 20;

        // 10
        int a5 = 20;

        // 11
        int 5a = 20;

        // 12
        string favorite_food = "Hamburger";

        // 13
        string FavoriteFood = "Hamburger";

        // 14
        string favorite food = "Hamburger";

        // 15
        int if = 7;

        // 16
        int else = 5;

        // 17
        int for = 6;
        
        // 18
        int a = 5, b = 6, c = 7;
        // Yes

        // 19
        int a, b, c = 5, 6, 7;
        // No

        // 20
        int a = 5; int b = 6; int c = 7;
        // Yes, because Java allows you to write multiple statements on the same line.

    return 0;
}

// reserved words for problems 15 - 17
// asm, auto, break, case, catch, char, class, const, continue, default, delete, do, double, else, enum, extern, float, for, friend, goto, if, inline, int, long, new, operator, private, protected, public, register, return, short, signed, sizeof, static, struct, switch, template, this, throw, try, typedef, union, unsigned, virtual, void, volatile, while
